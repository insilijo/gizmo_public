# GIZMO Paper 1 — core runtime
# Python 3.10+ required.

# numerics
numpy>=1.24,<2.0
scipy>=1.10,<2.0
pandas>=2.0,<3.0

# graph
networkx>=3.0,<4.0

# unsupervised + classification
scikit-learn>=1.3,<2.0

# multi-omic factor model (MOFA+ comparison in §4)
mofapy2>=0.7,<1.0

# figures
matplotlib>=3.7,<4.0
Pillow>=10.0,<12.0       # PIL — composite figure assembly

# optional, for some diagnostics
statsmodels>=0.14,<1.0   # Mann-Whitney + permutation null
