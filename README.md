# GIZMO — public repository for Paper 1

A biochemistry substrate as a fixed coordinate system for multi-omic per-patient projection.

This repository contains the public-facing artifacts of GIZMO Paper 1: the substrate, the framework code, the manuscript, the figures, and the per-cohort case-study deposit machinery. The 38,148-node biochemistry substrate (`substrate/graph.json`) is the primary contribution and is licensed CC-BY 4.0.

---

## Contents

- **[MANUSCRIPT.md](MANUSCRIPT.md)** — the Paper 1 manuscript (v5 draft).
- **[PRE_SUBMISSION_CHECKLIST.md](PRE_SUBMISSION_CHECKLIST.md)** — work remaining before submission (pre-registration Zenodo deposit, Supp Table S2, external validation citations).
- **`substrate/graph.json`** — the 38,148-node GIZMO biochemistry substrate (Reactome + StringDB + HMDB + KEGG, CC-BY 4.0). Load via `gizmo.export.json_export.read_json`. This is the primary deposit of the paper; downstream methods consume it.
- **`figures/`** — manuscript figures: 8 main + supplementary panels + per-pair chord co-structure PNGs. `figures/FIGURE_MANIFEST.md` indexes the lot.
- **`gizmo/`** — Python package implementing MAP reconstruction, β/α decomposition, signed-basin extraction, and the supporting graph/scoring/integration machinery.
- **`benchmarks/`** — analysis code that produced the manuscript numbers:
  - `make_zenodo_bundle.py` — assembles per-cohort case-study bundles for Zenodo deposit.
  - `baseline_mofa_streaming.py` — memory-bounded MOFA+ runs (IncrementalPCA single-block + subsample MOFA+ multi-block).
  - `baseline_mofa_with_weights.py` / `baseline_mofa_extended_cohorts.py` — standard MOFA+ comparison.
  - `per_patient_master.py` — per-cohort data loaders.
  - `diagnostics/multi_pc_vs_mofa_*.py` — symmetric multi-axis discrimination comparison.
  - `diagnostics/multi_pc_variance_bonferroni.py` — Bonferroni audit augmenter.
  - `diagnostics/axis_metadata_extended.py` — per-cohort clinical-metadata loaders.
  - `diagnostics/luad_gof_verification.py` — GoF scope-limit falsification.
  - `diagnostics/reaction_contraction_test.py` — substrate-validation contraction test.
  - `figures/build_*.py` — figure builders (chord, chord-costructure, signed-basin, basin pairs).
- **`results/`** — manuscript-source data tables (substrate-matched MOFA+ comparison, PC alignment, pass-rate disclosure).

---

## Quick start

```bash
# 1. Install dependencies
pip install -r requirements.txt    # numpy, scipy, networkx, sklearn, pandas, matplotlib, mofapy2

# 2. Load the substrate
python3 -c "
from gizmo.export.json_export import read_json
mg = read_json('substrate/graph.json')
print(f'{mg.graph.number_of_nodes()} nodes, {mg.graph.number_of_edges()} edges')
"

# 3. (optional) Rebuild the Zenodo per-cohort case-study deposit
#    Requires cohort F matrices + MOFA+ weights cached locally (these are
#    in the Zenodo deposit when published; not redistributed here under
#    each cohort's original source-data license).
python3 benchmarks/make_zenodo_bundle.py --dry-run
```

---

## What the substrate is

A fixed 38,148-node biochemistry graph (16,343 genes + 6,406 metabolites + 15,399 reactions + ancillary), merged from four curated sources:

- **Reactome** — 15,399 reactions, 6,406 metabolites, 2,382 pathway nodes
- **StringDB** — protein-protein interactions (confidence ≥ 700)
- **HMDB** — metabolite identifiers and synonyms
- **KEGG** — reaction directions and EC-class annotations

Any patient's multi-omic measurements MAP-project onto this substrate, producing a per-patient state vector **F** in 38,148-dim substrate coordinates that don't depend on cohort or modality. The substrate is the coordinate system; downstream method choices (GIZMO's MAP + β/α decomposition + signed basins, or alternatives like static PageRank / MOFA+ / PCA / NMF) operate within it.

See **[MANUSCRIPT.md](MANUSCRIPT.md)** §1 for full substrate construction, and `substrate/README.md` for a focused substrate-only intro.

---

## Per-cohort case-study deposit (Zenodo)

Every cohort GIZMO has been applied to is deposited at Zenodo with the full analytical bundle: F matrix, β/α decomposition, signed-basin output, MOFA+ comparison weights, per-cohort metadata, and an auto-populated case-study writeup. The bundle is regenerated by `benchmarks/make_zenodo_bundle.py`. See **[PRE_SUBMISSION_CHECKLIST.md](PRE_SUBMISSION_CHECKLIST.md)** for upload status.

Zenodo DOI: *TBD on acceptance.*

---

## Paper series

This repository is the Paper 1 deposit. Subsequent papers extend the substrate and exploit it in further ways:

- **Paper 2** — lipid layer dual-architecture (flux + state sub-graphs, compositional Bayes for ionization-bias correction), degree-weighted MAP.
- **Paper 3** — disease-informed topology rewiring (network deformation under disease state rather than fixed substrate).
- **Paper 4** — mutation-conditional GoF edge injection (the explicit fix for the LoF/GoF scope limit identified in §5).
- **Paper 5** — longitudinal + spatial-tile projection (architectural enablement from substrate-fixed coordinates).

---

## License

- **Substrate + analytical outputs**: CC-BY 4.0.
- **Code**: Apache 2.0.
- **Upstream source data** (per-cohort raw inputs): governed by each source's original license; not redistributed here. See **[LICENSE](LICENSE)**.

If you use the substrate or code, please cite:

> Gardner JJ. *A biochemistry substrate as a fixed coordinate system for multi-omic per-patient projection.* Manuscript in preparation (Zenodo DOI on acceptance).

Plus the four substrate sources (Reactome, StringDB, HMDB, KEGG; see [LICENSE](LICENSE)).

---

## Contact

Joseph J. Gardner (Insilijo, independent researcher) — joseph.j.gardner@gmail.com

Issues + pull requests at this repository.
